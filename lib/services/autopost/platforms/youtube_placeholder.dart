class YouTubeAutopostClient {
  Future<void> post({required Map<String, Object?> payload}) async {
    // TODO: Implement YouTube Data API integration (server-side recommended).
    // Keep API keys off the client; use Firebase Functions instead.
    return;
  }
}
